# DEPRECATED — sustituido por docker-compose.override.yml (carga automática).
#
# Antes: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d
# Ahora:  docker compose up -d --build
#
# Producción: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

include:
  - path: docker-compose.override.yml
